---
- name: Creates directory
  file:
    path: ~/backend
    state: directory

- name: Zip dist
  archive:
    path: /root/project/backend/dist
    dest:  /root/project/backend/dist.zip
    format: zip

- name: Copy dist
  become: true
  copy:
    src: "/root/project/backend/dist.zip"
    dest: "~/backend/dist.zip"
  
- name: Unzip dists
  unarchive:
    src: ~/backend/dist.zip
    dest: ~/backend/
    remote_src: yes






     

    